// 모바일 세로 화면을 기준으로 한 앱 뷰포트 최대 너비.
// 데스크톱 브라우저에서도 이 너비를 넘지 않는 세로 프레임 안에서만 렌더링한다.
export const PORTRAIT_VIEWPORT_MAX_WIDTH_PX = 480;

// 스플래시 → 로비 전환에 쓰이는 페이드 아웃/페이드 인 트랜지션 시간.
export const FADE_TRANSITION_MS = 400;
